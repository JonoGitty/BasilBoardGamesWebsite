{
  "category": "auth",
  "rules": [
    {
      "pattern": "session.*expired|JWT.*expired|token.*expired|invalid.*refresh",
      "code": "AUTH_SESSION_EXPIRED",
      "confidence": 0.9
    },
    {
      "pattern": "missing.*token|no.*auth|authorization.*header|not.*authenticated",
      "code": "AUTH_MISSING_TOKEN",
      "confidence": 0.85
    },
    {
      "pattern": "unauthorized|403|forbidden|not.*admin|insufficient.*role|permission.*denied",
      "code": "AUTH_UNAUTHORIZED",
      "confidence": 0.85
    },
    {
      "pattern": "signup.*failed|already.*registered|weak.*password|invalid.*email.*format",
      "code": "AUTH_SIGNUP_FAILED",
      "confidence": 0.8
    }
  ]
}
