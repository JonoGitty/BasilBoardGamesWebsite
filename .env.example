# Supabase – create a project at https://supabase.com/dashboard
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public app URL – used for email confirmation redirect links.
# In production this is set via GitHub Actions secrets.
# Locally, leave blank to use window.location.origin automatically.
# VITE_PUBLIC_APP_URL=https://basilboardgames.co.uk

# Service role key is NOT a VITE_ var — used only by edge functions
# and GitHub Actions (stored as a repository secret).
# SUPABASE_SERVICE_ROLE_KEY=
