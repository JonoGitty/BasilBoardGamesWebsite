# Supabase – create a project at https://supabase.com/dashboard
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Service role key is NOT a VITE_ var — used only by edge functions
# and GitHub Actions (stored as a repository secret).
# SUPABASE_SERVICE_ROLE_KEY=
